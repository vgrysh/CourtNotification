# Cloud Run injects PORT; locally defaults to 8080
server.port=${PORT:8080}

# Set via Cloud Run env vars: TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID
# Can also be entered through the UI (stored in memory only)
telegram.bot.token=${TELEGRAM_BOT_TOKEN:}
telegram.chat.id=${TELEGRAM_CHAT_ID:}
#a=b